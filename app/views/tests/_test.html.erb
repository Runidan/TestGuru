<tr>
  <td><%= test.title %> (<%= test.questions.count %>)</td>
  <td class="text-center"><%= test_level(test) %></td>
  <td class="text-center" ><%= button_to t('.start'), start_test_path(test), class: 'btn btn-outline-primary btn-sm', type: 'button' %></td>
</tr>
